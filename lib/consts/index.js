const englishMonths = require( './englishMonths' )
const holidays = require( './holidays' )
const months = require( './months' )
const movableHolidays = require( './movableHolidays' )
const nakshatras = require( './nakshatras' )
const numbers = require( './numbers' )
const pakshNames = require( './pakshNames' )
const tithiNames = require( './tithiNames' )
const weekdays = require( './weekdays' )

module.exports = {
  englishMonths,
  holidays,
  months,
  movableHolidays,
  nakshatras,
  numbers,
  pakshNames,
  tithiNames,
  weekdays,
}
