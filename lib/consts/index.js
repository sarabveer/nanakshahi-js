const englishMonths = require( './englishMonths' )
const gurpurabs = require( './gurpurabs' )
const months = require( './months' )
const movableGurpurabs = require( './movableGurpurabs' )
const weekdays = require( './weekdays' )

module.exports = {
  englishMonths,
  gurpurabs,
  months,
  movableGurpurabs,
  weekdays,
}
